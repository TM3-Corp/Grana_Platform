[phases.setup]
nixPkgs = ['python312']

[phases.install]
cmds = ['pip install -r backend/requirements.txt']

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT --app-dir backend --workers 2 --timeout-keep-alive 65'
